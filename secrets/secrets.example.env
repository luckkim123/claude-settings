# Copy this file to secrets/secrets.env (gitignored) and fill in real values.
# install.sh reads it to substitute ${VAR} placeholders in claude/mcp.template.json.

# --- Smithery (https://smithery.ai) ---
SMITHERY_API_KEY=your-smithery-api-key
SMITHERY_PROFILE=your-smithery-profile

# Add new secrets below as you introduce more MCP servers.
# Reference them in claude/mcp.template.json as ${VAR_NAME}.
